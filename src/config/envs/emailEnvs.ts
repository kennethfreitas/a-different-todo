export const EMAIL_ENV = {
  HOST: process.env.EMAIL_HOST!,
  PORT: +process.env.EMAIL_PORT!,
  USER: process.env.EMAIL_USER!,
  PASSWORD: process.env.EMAIL_PASSWORD!,
  ORIGIN_EMAIL: process.env.EMAIL_ORIGIN_EMAIL!,
};
